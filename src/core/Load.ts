// import resConfig from '../../assets/configs/res';
// let resConfig: any;
/**
 * 加载资源文件
 */
export default class Load {
    // static RES_BASE = '../assets/';

    // /**
    //  * 加载分组资源
    //  * @param name 组名
    //  * @returns 
    //  */
    // static group<T = any>(name: keyof typeof resConfig) {
    //     const resGroup = resConfig[name];
    //     let key: keyof typeof resGroup;
    //     for (key in resGroup) {
    //         Assets.add(key, this.RES_BASE + resGroup[key]);
    //     }

    //     return Assets.load<T>(Object.keys(resGroup)) // => Promise<{flowerTop: Texture, eggHead: Texture}>;
    // }


}