import { Matrix } from "../../math";
import { Texture } from "../../texture/Texture";
import { Transform } from "../../transform";
/**
 * 纹理渲染
 */
export default function textureRender(ctx: CanvasRenderingContext2D, node: Transform, matrix: Matrix, texture: Texture): void;
